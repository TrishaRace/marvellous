package com.example.carrerap.marvellous

import android.os.Bundle
import android.support.v7.app.AppCompatActivity
import kotlinx.android.synthetic.main.activity_character_info.*
import com.example.carrerap.marvellous.model.Character

class InfoActivity: AppCompatActivity()  {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_character_info)

        //getString(CHARACTER_ID, character_id)

        //tv_character_info.text = character.name

    }
}